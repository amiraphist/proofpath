# ProofPath Authority Post Banner

## Selected source stage

Use **Build 06: Cross-Border Contractor**. It is an existing Build mission and its genuine hardened route uses eight game nodes:

`AI Agent → Policy guard → Verify recipient → Check payment → Spending limit → Owner approval → Ledger Nano™ Gen5 → Send payment`

## Verified replay evidence

The real replay created seven blue pen lines, showed the Stage 06 VERIFIED stamp, and returned `PAYMENT PLAN VERIFIED`, `HARDENED`, and score `100`.

## Capture finding

The first screenshot captured the complete game UI successfully. A later attempt to remove the environment's preview-host footer hid an ancestor that also contained the app, creating an unusable white capture. Do not hide ancestors by text matching; retain the original real game capture and crop only the outer preview bar with screenshot coordinates.

## Build 07 banner check

Build 07 replay is genuine and verified, with five nodes and four blue pen lines. The first cleaned viewport composition removed side panels successfully, but the initial node targets were based on the full layout and left the first and final nodes clipped after the central board expanded. The capture must be replayed with the five nodes positioned relative to the widened board before it is delivered.

The balanced replay now keeps every node inside the board in the intended order: AI Agent → Check payment → Multisig quorum → Ledger Nano™ Gen5 → Send payment. It carries four authentic blue pen connections, a VERIFIED result, score 100, and an on-screen Nodes control. Before release, retain only a deliberately styled Nodes control or hide it; an unstyled desktop-forced mobile button is not suitable for the final image.

## Final capture

The final 1600×830 PNG uses the real Build 07 board and real session state. The five node route is centered before execution, so verification remains present: `AI Agent → Check payment → Multisig quorum → Ledger Nano™ Gen5 → Send payment`. The screenshot visibly contains the four blue pen lines, the authentic **VERIFIED** stamp, the score of **100**, the `Next mission` control, and the game’s glass **Nodes** orb. The Ledger device is the game’s official Jet Black SVG asset; no gameplay UI was generated or recreated outside the running game.
